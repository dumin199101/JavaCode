package com.goosuu.code05;

/**
 *  JSTL:
 *    1.if标签(if)
 *        属性：test
 *    2.choose标签(switch)
 *      when标签(case)
 *      otherwise标签(default)
 *    3.forEach标签(for)
 *      属性：begin,end,step,var,varStatus,items
 *           varStatus:
 *              index:索引，从0开始
 *              count:循环次数
 *
 *
 */
public class JSTLDemo {
}
